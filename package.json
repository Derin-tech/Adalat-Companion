{
  "name": "adalat-companion",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix frontend && mkdir -p dist && cp -r frontend/dist/* dist/"
  },
  "dependencies": {
    "axios": "^1.20.0",
    "cheerio": "^1.2.0",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "form-data": "^4.0.6",
    "multer": "^2.3.0",
    "nodemailer": "^10.0.0",
    "pdf-parse": "^2.4.5",
    "uuid": "^14.0.2"
  }
}
